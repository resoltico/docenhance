// SPDX-FileCopyrightText: 2026 Ervins Strauhmanis
// SPDX-License-Identifier: MIT
// Generated by tools/generate_spec.py. Edit spec/cli-contract.json instead.
#pragma once
#include "docenhance/contract/command.hpp"

#include <array>
#include <string_view>
namespace docenhance::contract {
inline constexpr std::string_view contract_status = "target-1.0; processing not implemented";
// The usage line of every command, as the reviewed contract states it.
[[nodiscard]] constexpr std::string_view command_usage(Command command) noexcept {
    switch (command) {
    case Command::root:
        return "docenhance COMMAND [OPTIONS]";
    case Command::process:
        return "docenhance process INPUT --out-dir DIRECTORY [OPTIONS]";
    case Command::plan:
        return "docenhance plan INPUT [OPTIONS]";
    case Command::inspect:
        return "docenhance inspect INPUT [OPTIONS]";
    case Command::presets:
        return "docenhance presets [NAME] [--json]";
    case Command::methods:
        return "docenhance methods [ID] [--json]";
    case Command::version:
        return "docenhance version [--json]";
    }
    return {};
}
struct OptionDescriptor {
    std::string_view name;
    std::string_view metavar;
    CommandSet scope;
    std::string_view group;
    std::string_view domain;
    std::string_view methods;
    std::string_view description;
};
// Descriptions are long single-line literals; keep one option per line.
// clang-format off
inline constexpr auto option_catalog = std::to_array<OptionDescriptor>({
    {.name = "--out-dir", .metavar = "PATH", .scope = CommandSet{Command::process, Command::plan}, .group = "Selection and runtime arguments", .domain = "Required for P; optional for L", .methods = "", .description = "New result directory. Parent must exist. Final path must not exist, including as a symlink. No implicit parent creation."},
    {.name = "--recursive", .metavar = "", .scope = CommandSet{Command::process, Command::plan, Command::inspect}, .group = "Selection and runtime arguments", .domain = "False", .methods = "", .description = "Traverse input directory descendants under the discovery rules. Error for file input."},
    {.name = "--pages", .metavar = "SELECTION", .scope = CommandSet{Command::process, Command::plan, Command::inspect}, .group = "Selection and runtime arguments", .domain = "`all`", .methods = "", .description = "File input only. `all` or comma-separated positive integers/inclusive ranges; no spaces, duplicates, zero, reversed or open-ended ranges. Selection must exist. Processing order is ascending page number."},
    {.name = "--preset", .metavar = "NAME", .scope = CommandSet{Command::process, Command::plan}, .group = "Selection and runtime arguments", .domain = "`balanced` when no recipe", .methods = "", .description = "`none`, `balanced`, `shadow`, `faint`, `noisy`. Explicitly incompatible with `--recipe`."},
    {.name = "--recipe", .metavar = "PATH", .scope = CommandSet{Command::process, Command::plan}, .group = "Selection and runtime arguments", .domain = "Absent", .methods = "", .description = "Strict complete JSON RecipeV1, maximum 2 MiB. No automatic user/system config files."},
    {.name = "--protect-mask", .metavar = "PATH", .scope = CommandSet{Command::process, Command::plan}, .group = "Selection and runtime arguments", .domain = "Absent", .methods = "", .description = "Single selected page only; protection contract in \u00a75.9. Not a recipe path field."},
    {.name = "--on-error", .metavar = "MODE", .scope = CommandSet{Command::process, Command::plan}, .group = "Selection and runtime arguments", .domain = "`stop`", .methods = "", .description = "`stop` aborts before publication on any file/page failure. `continue` permits a partial bundle if at least one page succeeds. Fatal run/publication errors always abort."},
    {.name = "--threads", .metavar = "VALUE", .scope = CommandSet{Command::process, Command::plan}, .group = "Selection and runtime arguments", .domain = "`auto`", .methods = "", .description = "`auto` = min(4,max(1,reported hardware concurrency)); or integer 1\u201364. Internal numerical parallelism, not multiple simultaneous pages."},
    {.name = "--memory-mib", .metavar = "N", .scope = CommandSet{Command::process, Command::plan}, .group = "Selection and runtime arguments", .domain = "1024; integer 256\u201365536", .methods = "", .description = "Working-allocation planning budget; not a promised hard RSS ceiling. 1 MiB = 1,048,576 bytes."},
    {.name = "--max-megapixels", .metavar = "N", .scope = CommandSet{Command::process, Command::plan}, .group = "Selection and runtime arguments", .domain = "40; finite 1\u2013250", .methods = "", .description = "Maximum pixels per decoded source page and final geometry canvas; 1 MP = 1,000,000 pixels. No automatic resize to satisfy it."},
    {.name = "--max-input-mib", .metavar = "N", .scope = CommandSet{Command::process, Command::plan}, .group = "Selection and runtime arguments", .domain = "1024; integer 1\u20138192", .methods = "", .description = "Maximum encoded bytes in one source file, before snapshot/decode."},
    {.name = "--progress", .metavar = "MODE", .scope = CommandSet{Command::process, Command::plan}, .group = "Selection and runtime arguments", .domain = "`auto`", .methods = "", .description = "`auto` emits stderr progress only when stderr is a terminal; `plain` always emits lines; `none` disables it. Plan reports header progress only."},
    {.name = "--quiet", .metavar = "", .scope = CommandSet{Command::process, Command::plan}, .group = "Selection and runtime arguments", .domain = "False", .methods = "", .description = "Suppress informational stderr and progress, never the final result or errors. Overrides progress display only."},
    {.name = "--json", .metavar = "", .scope = CommandSet::all(), .group = "Selection and runtime arguments", .domain = "False", .methods = "", .description = "Exactly one final stdout JSON object. `presets NAME --json` emits RecipeV1."},
    {.name = "--help", .metavar = "", .scope = CommandSet::all(), .group = "Selection and runtime arguments", .domain = "N/A", .methods = "", .description = "Help and exit zero; no processing."},
    {.name = "--version", .metavar = "", .scope = CommandSet{Command::root}, .group = "Selection and runtime arguments", .domain = "N/A", .methods = "", .description = "Human-readable alias of `version`."},
    {.name = "--output-mode", .metavar = "MODE", .scope = CommandSet{Command::process, Command::plan}, .group = "Output and interpretation arguments", .domain = "`preserve`; `preserve|gray|bw`", .methods = "Final representation. `bw` activates a binarizer, default Sauvola unless explicitly chosen.", .description = "Final representation. `bw` activates a binarizer, default Sauvola unless explicitly chosen."},
    {.name = "--format", .metavar = "FORMAT", .scope = CommandSet{Command::process, Command::plan}, .group = "Output and interpretation arguments", .domain = "`png`; `png|tiff`", .methods = "Lossless per-page output container; not inferred from `--out-dir`.", .description = "Lossless per-page output container; not inferred from `--out-dir`."},
    {.name = "--bit-depth", .metavar = "DEPTH", .scope = CommandSet{Command::process, Command::plan}, .group = "Output and interpretation arguments", .domain = "`auto`; `auto|1|8|16`", .methods = "Auto chooses 1 for bw, otherwise 16 if the decoded source carries 16-bit precision, else 8. Depth 1 requires bw. Explicit 8/16 with bw encodes only the two endpoint values.", .description = "Auto chooses 1 for bw, otherwise 16 if the decoded source carries 16-bit precision, else 8. Depth 1 requires bw. Explicit 8/16 with bw encodes only the two endpoint values."},
    {.name = "--alpha", .metavar = "MODE", .scope = CommandSet{Command::process, Command::plan}, .group = "Output and interpretation arguments", .domain = "`white`; `white|black|reject`", .methods = "\u00a75.6 compositing policy.", .description = "\u00a75.6 compositing policy."},
    {.name = "--profile-policy", .metavar = "MODE", .scope = CommandSet{Command::process, Command::plan}, .group = "Output and interpretation arguments", .domain = "`embedded`; `embedded|srgb`", .methods = "\u00a75.3 color interpretation; `srgb` is an explicit profile override.", .description = "\u00a75.3 color interpretation; `srgb` is an explicit profile override."},
    {.name = "--rotate", .metavar = "DEGREES", .scope = CommandSet{Command::process, Command::plan}, .group = "Geometry arguments", .domain = "0; `0|90|180|270`", .methods = "G02", .description = "Exact clockwise quarter-turn after metadata orientation."},
    {.name = "--quad", .metavar = "POINTS", .scope = CommandSet{Command::process, Command::plan}, .group = "Geometry arguments", .domain = "Absent", .methods = "G03", .description = "Four normalized points `TL;TR;BR;BL`, each `x,y` in `[0,1]`, in the frame after `--rotate`. Requires one selected page and `--page-size`."},
    {.name = "--page-size", .metavar = "WIDTHxHEIGHT", .scope = CommandSet{Command::process, Command::plan}, .group = "Geometry arguments", .domain = "Absent; each integer 16\u201350000", .methods = "G03", .description = "Explicit rectified output pixel dimensions. Requires `--quad`; resource limits still apply."},
    {.name = "--deskew", .metavar = "VALUE", .scope = CommandSet{Command::process, Command::plan}, .group = "Geometry arguments", .domain = "`off`; `off|auto|ANGLE`", .methods = "G04", .description = "`ANGLE` is a finite clockwise correction in degrees, `[-15,15]`. Numeric zero means an intentional no-op."},
    {.name = "--deskew-limit", .metavar = "DEGREES", .scope = CommandSet{Command::process, Command::plan}, .group = "Geometry arguments", .domain = "5; finite `(0,15]`", .methods = "G04 auto only", .description = "Half-width of the automatic search interval. Not a clipping operation on a larger estimated angle."},
    {.name = "--dewarp", .metavar = "METHOD", .scope = CommandSet{Command::process, Command::plan}, .group = "Geometry arguments", .domain = "`off`; `off|text-lines`", .methods = "G05", .description = "Explicit vertical text-line dewarping. No horizontal justification or borrowed page model."},
    {.name = "--dewarp-min-lines", .metavar = "N", .scope = CommandSet{Command::process, Command::plan}, .group = "Geometry arguments", .domain = "8; integer 8\u201340", .methods = "G05", .description = "Minimum accepted long text lines for model construction."},
    {.name = "--dewarp-max-displacement", .metavar = "F", .scope = CommandSet{Command::process, Command::plan}, .group = "Geometry arguments", .domain = "0.08; finite `[0.01,0.15]`", .methods = "G05", .description = "Maximum absolute vertical displacement divided by canvas height, after boundary taper."},
    {.name = "--illumination", .metavar = "METHOD", .scope = CommandSet{Command::process, Command::plan}, .group = "Illumination arguments", .domain = "Preset; `off|auto|surface|morph`", .methods = "I01/I02", .description = "`auto` uses I01's estimator and exact automatic eligibility checks. It never substitutes I02."},
    {.name = "--background-strength", .metavar = "A", .scope = CommandSet{Command::process, Command::plan}, .group = "Illumination arguments", .domain = "0.35; finite `[0,1]`", .methods = "I01/I02", .description = "Exponent/blend strength of the bounded gain field, not a sharpening amount. Zero is a no-op."},
    {.name = "--background-max-gain", .metavar = "G", .scope = CommandSet{Command::process, Command::plan}, .group = "Illumination arguments", .domain = "1.5; finite `[1,4]`", .methods = "I01/I02", .description = "Hard upper bound on the proposed luminance multiplier before gamut-safe transport."},
    {.name = "--background-target", .metavar = "VALUE", .scope = CommandSet{Command::process, Command::plan}, .group = "Illumination arguments", .domain = "`source`; `source` or finite `[0.1,1]`", .methods = "I01/I02", .description = "`source` = 90th percentile of valid background estimates. Numeric value is a linear-light target. No automatic pure-white paper target."},
    {.name = "--background-cell", .metavar = "VALUE", .scope = CommandSet{Command::process, Command::plan}, .group = "Illumination arguments", .domain = "`auto`; integer 8\u2013512", .methods = "I01/auto", .description = "Full-canvas cell edge in pixels. Auto = clamp(round(min(W,H)/24),16,256)."},
    {.name = "--background-quantile", .metavar = "Q", .scope = CommandSet{Command::process, Command::plan}, .group = "Illumination arguments", .domain = "0.90; finite `[0.75,0.99]`", .methods = "I01/auto", .description = "Bright-sample quantile within each eligible cell."},
    {.name = "--background-smooth", .metavar = "BETA", .scope = CommandSet{Command::process, Command::plan}, .group = "Illumination arguments", .domain = "2.0; finite `[0.1,20]`", .methods = "I01/auto", .description = "Graph smoothness coefficient of the log-surface solve."},
    {.name = "--background-radius", .metavar = "VALUE", .scope = CommandSet{Command::process, Command::plan}, .group = "Illumination arguments", .domain = "`auto`; integer 1\u2013256", .methods = "I02 only", .description = "Morphological radius; square side `2r+1`. Auto = clamp(round(min(W,H)/50),8,128)."},
    {.name = "--denoise", .metavar = "METHOD", .scope = CommandSet{Command::process, Command::plan}, .group = "Denoising arguments", .domain = "Preset; `off|nlm|tvl1`", .methods = "D01/D02", .description = "Select exactly one denoiser."},
    {.name = "--denoise-blend", .metavar = "A", .scope = CommandSet{Command::process, Command::plan}, .group = "Denoising arguments", .domain = "0.5; finite `[0,1]`", .methods = "D01/D02", .description = "Convex blend of stage input and denoised candidate. Zero is a no-op."},
    {.name = "--nlm-h", .metavar = "H", .scope = CommandSet{Command::process, Command::plan}, .group = "Denoising arguments", .domain = "3.0; finite `[0.1,25]`", .methods = "D01", .description = "Strength in equivalent 8-bit perceptual intensity points. Native 16-bit strength = `257*H`."},
    {.name = "--nlm-patch", .metavar = "N", .scope = CommandSet{Command::process, Command::plan}, .group = "Denoising arguments", .domain = "7; odd integer 3\u201315", .methods = "D01", .description = "Patch width and height, pixels."},
    {.name = "--nlm-search", .metavar = "N", .scope = CommandSet{Command::process, Command::plan}, .group = "Denoising arguments", .domain = "21; odd integer 7\u201341", .methods = "D01", .description = "Search width and height, pixels; must be \u2265 patch size and fit the image's smaller dimension."},
    {.name = "--tv-lambda", .metavar = "LAMBDA", .scope = CommandSet{Command::process, Command::plan}, .group = "Denoising arguments", .domain = "1.5; finite `[0.05,20]`", .methods = "D02", .description = "Weight of the L1 fidelity term in `TV(u)+lambda*|u-f|_1`; **larger means less smoothing**."},
    {.name = "--tv-iterations", .metavar = "N", .scope = CommandSet{Command::process, Command::plan}, .group = "Denoising arguments", .domain = "150; integer 10\u20131000", .methods = "D02", .description = "Maximum primal-dual iterations."},
    {.name = "--tv-tolerance", .metavar = "EPS", .scope = CommandSet{Command::process, Command::plan}, .group = "Denoising arguments", .domain = "0.00001; finite `[1e-8,1e-3]`", .methods = "D02", .description = "Both primal and dual update tolerances; tested every 10 iterations after iteration 20."},
    {.name = "--deblur", .metavar = "METHOD", .scope = CommandSet{Command::process, Command::plan}, .group = "Deblurring arguments", .domain = "`off`; `off|wiener`", .methods = "R01", .description = "Known-PSF regularized restoration; never enabled by a preset."},
    {.name = "--psf", .metavar = "KIND", .scope = CommandSet{Command::process, Command::plan}, .group = "Deblurring arguments", .domain = "Required when enabling R01 without recipe PSF; `gaussian|motion|kernel`", .methods = "R01", .description = "Blur model in the post-geometry processing coordinate system."},
    {.name = "--psf-sigma", .metavar = "SIGMA", .scope = CommandSet{Command::process, Command::plan}, .group = "Deblurring arguments", .domain = "1.0; finite `[0.3,5]`", .methods = "Gaussian PSF", .description = "Standard deviation, pixels. Kernel radius = ceil(3*sigma)."},
    {.name = "--psf-length", .metavar = "LENGTH", .scope = CommandSet{Command::process, Command::plan}, .group = "Deblurring arguments", .domain = "5.0; finite `[1,31]`", .methods = "Motion PSF", .description = "Length of the exposure segment, pixels."},
    {.name = "--psf-angle", .metavar = "ANGLE", .scope = CommandSet{Command::process, Command::plan}, .group = "Deblurring arguments", .domain = "0; finite `[-180,180]`", .methods = "Motion PSF", .description = "Clockwise direction from positive x; negative allowed."},
    {.name = "--psf-file", .metavar = "PATH", .scope = CommandSet{Command::process, Command::plan}, .group = "Deblurring arguments", .domain = "Required for CLI `kernel` unless recipe contains coefficients", .methods = "Kernel PSF", .description = "Single grayscale PNG, unsigned 8/16-bit, odd dimensions 3\u2013129, no alpha. Raw samples are coefficients; ignore profile/gamma. Nonnegative, positive sum required."},
    {.name = "--wiener-k", .metavar = "K", .scope = CommandSet{Command::process, Command::plan}, .group = "Deblurring arguments", .domain = "0.01; finite `[1e-5,1]`", .methods = "R01", .description = "Regularization term in `|H|^2+K`; not a claimed measured noise-to-signal ratio."},
    {.name = "--deblur-blend", .metavar = "A", .scope = CommandSet{Command::process, Command::plan}, .group = "Deblurring arguments", .domain = "0.5; finite `[0,1]`", .methods = "R01", .description = "Blend of original linear luminance and the restored candidate."},
    {.name = "--contrast", .metavar = "METHOD", .scope = CommandSet{Command::process, Command::plan}, .group = "Contrast and sharpening arguments", .domain = "Preset; `off|levels|gamma|clahe`", .methods = "C01/C02/C03", .description = "Exactly one contrast method."},
    {.name = "--contrast-blend", .metavar = "A", .scope = CommandSet{Command::process, Command::plan}, .group = "Contrast and sharpening arguments", .domain = "1.0; finite `[0,1]`", .methods = "C01/C02/C03", .description = "Convex blend with the original perceptual plane."},
    {.name = "--levels-low", .metavar = "P", .scope = CommandSet{Command::process, Command::plan}, .group = "Contrast and sharpening arguments", .domain = "0.5; finite `[0,10]` percent", .methods = "C01", .description = "Lower percentile among valid unprotected samples."},
    {.name = "--levels-high", .metavar = "P", .scope = CommandSet{Command::process, Command::plan}, .group = "Contrast and sharpening arguments", .domain = "99.5; finite `[90,100]` percent", .methods = "C01", .description = "Upper percentile; must exceed lower percentile."},
    {.name = "--gamma", .metavar = "G", .scope = CommandSet{Command::process, Command::plan}, .group = "Contrast and sharpening arguments", .domain = "1.2; finite `[0.25,4]`", .methods = "C02", .description = "`f_new=f^G`; values above 1 darken midtones while retaining endpoints."},
    {.name = "--clahe-grid", .metavar = "CxR", .scope = CommandSet{Command::process, Command::plan}, .group = "Contrast and sharpening arguments", .domain = "`8x8`; each integer 2\u201332", .methods = "C03", .description = "Number of contextual columns/rows, not tile pixel dimensions. Each tile must have at least 16 pixels in each direction."},
    {.name = "--clahe-clip", .metavar = "C", .scope = CommandSet{Command::process, Command::plan}, .group = "Contrast and sharpening arguments", .domain = "2.0; finite `[1,8]`", .methods = "C03", .description = "Pre-redistribution histogram clip multiplier relative to average occupancy."},
    {.name = "--sharpen", .metavar = "METHOD", .scope = CommandSet{Command::process, Command::plan}, .group = "Contrast and sharpening arguments", .domain = "`off`; `off|unsharp`", .methods = "S01", .description = "Thresholded unsharp masking; never enabled by a preset."},
    {.name = "--sharpen-sigma", .metavar = "S", .scope = CommandSet{Command::process, Command::plan}, .group = "Contrast and sharpening arguments", .domain = "0.8; finite `[0.3,3]`", .methods = "S01", .description = "Gaussian blur standard deviation, pixels."},
    {.name = "--sharpen-amount", .metavar = "A", .scope = CommandSet{Command::process, Command::plan}, .group = "Contrast and sharpening arguments", .domain = "0.5; finite `[0,2]`", .methods = "S01", .description = "High-pass amplification."},
    {.name = "--sharpen-threshold", .metavar = "T", .scope = CommandSet{Command::process, Command::plan}, .group = "Contrast and sharpening arguments", .domain = "1.0; finite `[0,20]`", .methods = "S01", .description = "Soft-threshold in equivalent 8-bit perceptual intensity points; divide by 255 internally."},
    {.name = "--binarize", .metavar = "METHOD", .scope = CommandSet{Command::process, Command::plan}, .group = "Binarization arguments", .domain = "`sauvola` when bw is activated; `otsu|sauvola|fixed`", .methods = "B01/B02/B03", .description = "Requires `--output-mode bw`; does not implicitly change output mode."},
    {.name = "--sauvola-window", .metavar = "VALUE", .scope = CommandSet{Command::process, Command::plan}, .group = "Binarization arguments", .domain = "`auto`; odd integer 3\u2013511", .methods = "B02", .description = "Window side. Auto = `2*floor((v-1)/2+0.5)+1` for `v=0.015*min(W,H)` (nearest odd, ties upward), clamped 15\u2013101, then reduced to largest odd \u2264 min(W,H) when necessary."},
    {.name = "--sauvola-k", .metavar = "K", .scope = CommandSet{Command::process, Command::plan}, .group = "Binarization arguments", .domain = "0.20; finite `[0,1]`", .methods = "B02", .description = "Local threshold coefficient."},
    {.name = "--sauvola-r", .metavar = "R", .scope = CommandSet{Command::process, Command::plan}, .group = "Binarization arguments", .domain = "0.50; finite `[0.05,1]`", .methods = "B02", .description = "Standard-deviation scale in normalized perceptual intensity units."},
    {.name = "--fixed-threshold", .metavar = "T", .scope = CommandSet{Command::process, Command::plan}, .group = "Binarization arguments", .domain = "0.50; finite `[0,1]`", .methods = "B03", .description = "Black iff `f\u2264T`."},
});
// clang-format on
} // namespace docenhance::contract
