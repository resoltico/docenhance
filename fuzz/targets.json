{
  "$comment": "Per-target fuzzing settings, read by tools/run_fuzzers.py; the harness list is checked against the CMake targets, corpora and regressions by tools/check_project.py. libfuzzer_options are extra engine flags: the CLI target is slow (it builds the full parser per input), so it uses its whole length budget from the start instead of growing it.",
  "sanitizer_options": {
    "ASAN_OPTIONS": "abort_on_error=1:detect_stack_use_after_return=1:strict_string_checks=1:check_initialization_order=1:strict_init_order=1:allocator_may_return_null=0",
    "UBSAN_OPTIONS": "halt_on_error=1:print_stacktrace=1:report_error_type=1",
    "TSAN_OPTIONS": "halt_on_error=1:second_deadlock_stack=1"
  },
  "targets": {
    "pages": {
      "max_len": 4200
    },
    "decimal": {
      "max_len": 200
    },
    "numeric": {
      "max_len": 1100
    },
    "cli": {
      "max_len": 1300,
      "libfuzzer_options": [
        "-len_control=0"
      ]
    },
    "box_mean": {
      "max_len": 1300
    },
    "utf8": {
      "max_len": 4096
    }
  }
}
